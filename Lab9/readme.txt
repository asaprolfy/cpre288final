Some code for the volatile variables used by the interrupt handler has been updated to make it simpler. It now declares these as global variables and shares them across files using the extern modifier.

If you are just getting started or want to use the simpler version, see the “update” folder and use those files instead.